/node_modules/
.next/
out/
/dist/

.env
.env.local
.env.*.local
dist/.env
dist/standalone/.env
.data/

# Perfil persistente de Chromium/Chrome para "Iniciar sesión web" (cookies de
# sesión reales, no debe versionarse ni distribuirse con el instalador).
/.browser-profiles/

# Certificados de firma de codigo autofirmados: solo tienen valor en la
# maquina que los genero, no aportan nada al clonar el repo.
*.cer
*.pfx

build*.log
build-result.txt
curl-output.txt
search-trace-output.txt
borrar.txt
*.tsbuildinfo
# no aporta nada versionado.
/build/

# Artefactos de electron-builder que no forman parte de la distribución
# final (solo los .exe/instalador finales en dist/ se versionan).
dist/win-unpacked/
dist/builder-debug.yml
dist/latest.yml

# DiagnÃ³sticos locales que pueden contener tokens, cookies o respuestas reales.
/deepseek-dump.json
/ds-login.html
/test-deepseek.js

coverage/
*.blockmap
/scratch/
/claude-usage-widget-main/
Thumbs.db
*.tar
